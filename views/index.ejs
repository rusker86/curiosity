<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">

		<link rel="stylesheet" href="/styles.css">

		<title>Curiosity</title>
	</head>

	<body>
		<section>
			<h1>Bienvenido a Curiosity. Donde cada vez que entras aprendes algo nuevo</h1>
			
			<hr>

			<form action="/" method="post" id="categoryForm">
				<select name="category" id="category">
					<option value="all" <%= selectedCategory === 'all' ? 'selected' : '' %>>Todas</option>
					<option value="science" <%= selectedCategory === 'science' ? 'selected' : '' %>>Ciencia</option>
					<option value="history" <%= selectedCategory === 'history' ? 'selected' : '' %>>Historia</option>
					<option value="art" <%= selectedCategory === 'art' ? 'selected' : '' %>>Arte</option>
					<option value="curiosity" <%= selectedCategory === 'curiosity' ? 'selected' : '' %>>Curiosidad</option>
				</select>

			</form>

			<h2>¿Sabías qué...?</h2>

			<p> <q> <%= dato %> </q> </p>
		</section>

	</body>
	
	<script>
		const select = document.getElementById("category");
		const form = document.getElementById("categoryForm");

		select.addEventListener("change", () => {
			form.submit();
		});
	</script>
</html>